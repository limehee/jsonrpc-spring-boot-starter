rootProject.name = "jsonrpc-spring-boot-demo"

includeBuild("../..") {
    dependencySubstitution {
        substitute(module("io.github.limehee:jsonrpc-spring-boot-starter"))
                .using(project(":jsonrpc-spring-boot-starter"))
    }
}
